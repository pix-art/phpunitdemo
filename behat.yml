default:
    suites:
        functional:
            paths: [ %paths.base%/features ]
            contexts:
                - FeatureContext
    extensions:
        Behat\MinkExtension:
            base_url: http://localhost/
            sessions:
                default:
                    selenium2: ~