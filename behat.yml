default:
    suites:
        functional:
            paths: [ %paths.base%/features ]
            contexts:
                - FeatureContext
    extensions:
        Behat\MinkExtension:
            base_url: http://imd.demo.dev:8888/
            sessions:
                default:
                    selenium2: ~